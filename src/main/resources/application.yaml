spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/taskList
    username: postgres
    password: 39711
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: classpath:db/changelog/db.changelog.yaml
    enabled: true
    default-schema: public
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: true
#    Debug
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true

security:
  jwt:
    secret: bXkgand0IHRva2VuIG5ldmVyIGRlY29kZQ==
    access: 3600000
    refresh: 2592000000

server:
  servlet:
    context-path: /api/v1